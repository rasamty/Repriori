<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
  </ItemGroup>

  <!-- No global "using Xunit" here, on purpose (matches CalcSaMD's convention) — each
       test file states its own "using Xunit;" so it's self-contained and readable on
       its own, without depending on solution-wide implicit usings to make sense. -->

  <ItemGroup>
    <ProjectReference Include="..\..\src\Repriori.DocProfile\Repriori.DocProfile.csproj" />
  </ItemGroup>

</Project>